# API Specification (Placeholder)

## Endpoints

### `POST /api/v1/ingest`
Trigger ingestion of a GitHub repository.

### `GET /api/v1/graph?repo_id={id}`
Fetch the dependency and module graph data for the graph viewer.

### `POST /api/v1/chat`
Send messages to the AI agent. (May be upgraded to WebSockets later).

*To be expanded...*
